# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HU-Donations' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HU-Donations
	pod 'Firebase'
	pod 'Firebase/Core'
	pod 'Firebase/Auth'
	pod 'Firebase/Database'
	pod 'PayPal-iOS-SDK'
  target 'HU-DonationsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HU-DonationsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
